<% visiter = notification.visiter %>
<% product = notification.product %>

<div class="store-view clearfix ">
  <%= link_to user_user_path(notification.visiter) do %>
    <%= attachment_image_tag visiter, :image, :fill,100,100, format: "jpeg", fallback: "no_image.jpg", size: "50x50", class:"profile-img-circle" %>
  <% end %>
  <%= notification_form(notification) %><span class="moderate-font"><%= " (#{time_ago_in_words(notification.created_at)} 前)" %></span>
  
</div>