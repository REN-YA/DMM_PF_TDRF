<% if review.favorited_by?(current_user) %>
  <%= link_to user_review_favorite_path(review), method: :delete do %>
  <span style="color: red;">♥</span>
  <%= review.favorites.count %>
  <% end %>
<% else %>
  <%= link_to user_review_favorites_path(review) , method: :post do %>
  ♡<%= review.favorites.count %>
  <% end %>
<% end %>